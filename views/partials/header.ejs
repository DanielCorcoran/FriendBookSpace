<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<meta http-equiv="X-UA-Compatible" content="ie=edge">
	<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/2.1.3/jquery.min.js" async></script>
	<script src="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/3.3.7/js/bootstrap.min.js" async></script>
	<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
	<link rel="stylesheet" href="/stylesheets/main.css">
	<title>FriendBookSpace</title>
</head>

<body>
	<nav class="navbar navbar-default">
		<div class="container-fluid">
			<div class="navbar-header">
				<button type="button" class="navbar-toggle collapsed" data-toggle="collapse"
					data-target="#navbar-hamburger-menu" aria-expanded="false">
					<span class="sr-only">Toggle navigation</span>
					<span class="icon-bar"></span>
					<span class="icon-bar"></span>
					<span class="icon-bar"></span>
				</button>
				<a href="/" class="navbar-brand">FriendBookSpace</a>
			</div>
			<div class="collapse navbar-collapse">
				<% if (currentUser) { %>
				<ul class="nav navbar-nav navbar-right">
					<li><a href="/users/<%= currentUser._id %>">Signed in as <%= currentUser.username %></a></li>
					<li><a href="/logout">Logout</a></li>
				</ul>
				<form class="navbar-form navbar-right" action="/findusers" method="GET">
					<button class="btn btn-primary">Find people to follow</button>
				</form>
				<% } %>
			</div>

			<!-- The hamburger menu for small screens.  Since the format is slightly different from the navbar,
                        this is hudden until the menu pops up -->
			<div id="navbar-hamburger-menu" class="navbar-collapse collapse">
				<% if (currentUser) { %>
				<ul class="nav navbar-nav navbar-right visible-xs">
					<li><a href="/findusers">Find users to follow</a></li>
					<li><a href="/users/<%= currentUser._id %>">Profile</a></li>
					<li><a href="/logout">Logout</a></li>
				</ul>
				<% } %>
			</div>
		</div>
	</nav>

	<div id="flash-msg" class="container">
		<% if (error && error.length > 0) { %>
		<div class="alert alert-danger" role="alert">
			<%= error %>
		</div>
		<% } %>
		<% if (success && success.length > 0) { %>
		<div class="alert alert-success" role="alert">
			<%= success %>
		</div>
		<% } %>
	</div>